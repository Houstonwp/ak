[package]
name = "ak-random"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = { workspace = true }
rand_core = { version = "0.9.3", features = ["std"] }
rayon = { workspace = true }

[dev-dependencies]
criterion = "0.6"
rand = "0.9"

[[bench]]
name = "rng_bench"
harness = false
